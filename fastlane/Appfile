# App Store Connect / Apple Developer Portal credentials
# These will be provided via environment variables in CI

app_identifier("com.nopilepsy.app")
apple_id(ENV["APPLE_ID"] || "your@email.com")
team_id(ENV["TEAM_ID"] || "XXXXXXXXXX")
itc_team_id(ENV["ITC_TEAM_ID"] || "XXXXXXXXXX")
